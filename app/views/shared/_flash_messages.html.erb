<% [:notice, :error, :alert].each do |type| %>
   <% unless flash[type].blank? %>
     <div class="<%= bootstrap_flashmgs(type) %> fade in">
      <a href="#" data-dismiss="alert" class="close">×</a>
       <%= content_tag :p, flash[type] %>
     </div>
   <% end %>
<% end %>